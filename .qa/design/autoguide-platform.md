# Design: AutoGuide Platform

Epic design for the full AutoGuide Documentation Intelligence Engine.
Generated by `@feature-intake` from [docs/PRD.md](../../docs/PRD.md) and grill-me decisions.

## Problem & Intent

Developers building process-heavy apps (HR, operations, admin) ship faster than they document. AutoGuide binds documentation to the application itself — scanning code, runtime DOM, and Playwright traces to produce verifiable knowledge with provenance and confidence. The primary user (developer) installs, scans, reviews, and publishes documentation so employees need less manual training.

**Intent:** Deliver the full Combined Spec as architecture end-state, implemented in five vertical delivery layers with React as reference implementation and `integrations/hr-workflows` as the in-repo integration scenario.

## Non-Goals

- Hosted SaaS dependency for core features
- Replacing Playwright as test framework
- Modifying host app business logic without developer action
- Cloud AI by default for HR/person data
- Building all adapters before React reference cycle is proven

## Assumptions

- Solo developer + Cursor agents implement slices via `@ecc-runner`
- `integrations/hr-workflows` provides checked-in Playwright fixtures for CI (no external repos)
- Optional: browo-hr (`../browo-hr`) for manual realism checks when cloned locally
- pnpm monorepo, TypeScript strict, Vitest for unit tests
- JSON is source of truth; SQLite is index/cache only
- German UI copy; English code/commits

## Options Considered

### Option A: MVP-only first (rejected)
- Summary: Ship Help Widget + DOM scan only, defer Playwright/AI/adapters
- Pros: Faster first release
- Cons: Wow moment (step-by-step flows) not achievable; throws away spec architecture
- Evidence: Grill-me decision for full spec (c)

### Option B: Big-bang monolith (rejected)
- Summary: Single issue builds entire platform
- Pros: No integration seams
- Cons: Unreviewable, untestable, blocks ecc-runner pipeline
- Evidence: slice-rules.md max ~800 LOC per issue

### Option C: Layered vertical slices with interface-first core (chosen)
- Summary: Core interfaces → React reference → scanners → confidence → Playwright/AI → UI/publish → other adapters
- Pros: Each slice PR-sized; adapters reuse proven APIs; hr-workflows validates in CI
- Cons: Longer total calendar time
- Evidence: SPEC_FULL.md phases 1–8 map cleanly to layers

### Option D: YAGNI / defer everything except DOM inventory (Ponytail Rung 1)
- Summary: Only monorepo + DOM button list + Markdown dump
- Pros: Smallest possible start
- Cons: Does not meet wow moment or spec acceptance
- **Documented as Rung 1 alternative — not chosen for this epic**

## Decision

**Chosen:** Option C — layered vertical slices, full spec end-state, React + `integrations/hr-workflows` first.

**Why:** User locked scope (c), wow moment requires flows not inventory, and plugin architecture must be real from day one to avoid rework for Vue/Angular/Svelte/Tauri/Flutter.

## Cross-Domain Sign-Off

| Domain | Status | Note |
|--------|--------|------|
| KISS | ✅ | JSON SoT, thin adapters, no premature hosted sync |
| SOLID | ✅ | Core/adapters/scanners separated by boundary rules |
| DRY | ✅ | Shared graph/confidence in core; adapters bridge only |
| Security | ✅ | Cloud AI opt-in; API key + URL user-supplied; PII warning |
| UI/UX | ✅ | development/published modes; German copy |
| Scaling | ⚠️ | SQLite index OK for local; hosted sync deferred layer 5 |
| Testability | ✅ | Vitest + schema validation + integrations/hr-workflows fixtures |
| Maintainability | ✅ | Monorepo packages, plugin registry |

## Runtime Matrix

| Area | Local (CLI/SDK) | Cloud session | Tauri |
|------|-----------------|---------------|-------|
| Core / storage | ✅ primary | — | — |
| React adapter | ✅ primary | partial (SSR later) | — |
| Runtime scanner | ✅ browser | ✅ | layer 5 |
| Source scanner | ✅ | — | — |
| Playwright import | ✅ local traces | — | — |
| AI Ollama | ✅ localhost | — | — |
| AI cloud | opt-in key+URL | opt-in | opt-in |
| Help widget | ✅ | ✅ | layer 5 |
| Vue/Angular/Svelte | — | — | — |
| Flutter adapter | — | — | layer 5 |

## Confidence

**82%** — architecture clear; risk is scope size and adapter parity definition.

## Implementation Sketch

```
packages/core/          — Fact, Graph, Confidence, Verification, PluginRegistry
packages/config/        — autoguide.config.ts schema
packages/storage/       — JSON writer, SQLite index
packages/cli/           — init, scan, review, generate, export, doctor, publish
plugins/react/          — Provider, Widget, Inspector, DocElement
plugins/vue|angular|svelte/ — same runtime API as React
plugins/tauri|flutter/  — non-DOM runtime bridges
examples/react-vite/    — minimal reference host
integrations/hr-workflows/ — self-contained HR scenario (CI)
```

**Integration path:** `examples/react-vite` for minimal SDK demo; `integrations/hr-workflows` for 3-flow acceptance (fixtures in-repo)

## Ponytail Rung 1 (explicitly NOT in first issues)

- Flutter adapter
- Hosted sync, SSO, enterprise registry, analytics
- PDF/HTML export (Markdown first)
- sagadrive / scriptony external validation (until Tauri adapter exists)

## Open Questions

- ~~GitHub remote repo name/owner~~ — `iamthamanic/autoguide`
- ~~Exact 3 hr-workflows flows for acceptance~~ — Wiki, Stammdaten, Mitarbeiter flows (green in CI)

## Phase 3 roadmap (completion sync — 2026-07)

| Track | Issues (representative) | Status |
|-------|-------------------------|--------|
| Runtime & scan depth | #70 snapshot v2, #68 AST scanner, #72 flow verify | ✅ merged |
| Trust & publish | #71 review loop, #73 redaction, #79 JSON Schema | ✅ merged |
| CLI & extensibility | #83 generate, #81 plugins, #80 recommendations | ✅ merged |
| SDK & docs | #86 DocElement, #82 packages/ui, #84 public API docs | ✅ merged |
| CI & integration | #40 validate, #41 hr-workflows | ✅ green |
| Adapter parity | Vue/Angular/Svelte UI styleguide (#85) | ⏳ open |
| Doc truth sync | #87 PRD + acceptance sync | 🔄 this issue |
| Enterprise / hosted | sync, SSO, analytics | ⏳ deferred (Layer 5) |
| Tauri / Flutter dogfood | scriptony, Flutter example | ⏳ partial stubs |

**Pipeline queue after #87:** #85 → #74–#78 (per ecc-runner batch).

## Ready for /implement

YES — epic slices continue via `@ecc-runner`; documentation synced with main.
